# DSpark verification-step similarity outputs

The runner stores each experiment in `results/dspark_verfication_step_similarity/`. Each run creates a UTC timestamp child directory (`YYYYmmdd_HHMMSS`)
containing server/client logs, benchmark output, raw records, pair-level CSVs,
the distance summary and plot, and `report.md`. Runtime artifacts are ignored.
